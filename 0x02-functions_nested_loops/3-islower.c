#include "main.h"

/**
 * _is lower checks for character in lowercase
 * c -s the character being checked
 * Return: 1 if c is lowercase, else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
